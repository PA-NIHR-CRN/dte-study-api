namespace Domain.Entities.ParticipantRegistrations
{
    public enum ParticipantRegistrationStatus
    {
        // Invited means Screening
        None, Applied, Screening, NotSelected, Enrolled, NotEnrolled, Withdrawn
    }
}