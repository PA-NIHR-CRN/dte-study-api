@model BPOR.Rms.Models.Study.StudyFormViewModel

<form-group for="SinglePersonResponsibleForRecruiting">
    <radio-set for="SinglePersonResponsibleForRecruiting">
        <radio value="@true">Yes</radio>
        <radio value="@false">No</radio>
    </radio-set>
</form-group>