@model VsiEditModel

@{
    VsiEditContext context = ViewBag.Context;
    context.SectionName = Resources.Section2Name;
    ViewData.SetBackLinkOverride(Url.Action("Section2_Step6", new { id = context.Vsi.StudyId }));
    context.SkipUrl = Url.Action("Section3_Step1", new { id = context.Vsi.StudyId });
    Layout = "_VsiEditLayoutForm";
}

<form-group for="OtherDetails">
    <input class="govuk-input" asp-for="OtherDetails" type="text">
</form-group>