@using NIHR.GovUk.AspNetCore.Mvc
@model VsiEditModel

@{
    VsiEditContext context = ViewBag.Context;
    ViewData.SetBackLinkOverride(Url.FlowNext(context, "Section2_Step7"));
    ViewData.SetSectionName(Resources.Section3Name);
    ViewData.SetSkipLink(Url.FlowNext(context, "Section3_Step2"));
    Layout = "_VsiEditLayoutForm";
}

<form-group for="StagedPreScreenerUrl" label-class="govuk-heading-xl">
    <input class="govuk-input" asp-for="StagedPreScreenerUrl" type="text">
</form-group>