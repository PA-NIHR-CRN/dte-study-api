@model VsiEditModel

@{
    VsiEditContext context = ViewBag.Context;
    context.SectionName = Resources.Section2Name;
    Layout = "_VsiEditLayoutForm";
}

<form-group for="HasIncentive" label-class="govuk-heading-xl">
    <radio-set for="HasIncentive">
        <radio value="@true">Yes</radio>
        <radio value="@false">No</radio>
    </radio-set>
</form-group>

<form-group for="IncentiveDetails">
    <input class="govuk-input" asp-for="IncentiveDetails" type="text">
</form-group>