@model VsiEditModel

@{
    VsiEditContext context = ViewBag.Context;
    ViewData.SetBackLinkOverride(Url.FlowNext(context, "Section2_Step6"));
    ViewData.SetSectionName(Resources.Section2Name);
    ViewData.SetSkipLink(Url.FlowNext(context, "Section3_Step1"));
    Layout = "_VsiEditLayoutForm";
}

<form-group for="OtherDetails" label-class="govuk-heading-l">
    <textarea rows="5" class="govuk-textarea" asp-for="OtherDetails"></textarea>
</form-group>